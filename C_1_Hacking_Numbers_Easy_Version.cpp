#include<bits/stdc++.h>
using namespace std;
 
#define int ll
using ll = long long;
using pii = pair<int, int>;
 
int32_t main(){
    int t;
    cin>>t;
    while(t--){
        int n,x;
        cin>>n;
        cout<<"digit"<<endl;
        cout.flush();
        cin>>x;
        cout<<"digit"<<endl;
        cout.flush();
        cin>>x;
        cout<<"add "<<-8<<endl;
        cout.flush();
        cin>>x;
        cout<<"add "<<-4<<endl;
        cout.flush();
        cin>>x;
        cout<<"add "<<-2<<endl;
        cout.flush();
        cin>>x;
        cout<<"add "<<-1<<endl;
        cout.flush();
        cin>>x;

        cout<<"add "<<n-1<<endl;
        cout.flush();
        cin>>x;
        cout<<"!"<<endl;
        cout.flush();
        cin>>x;
    }
    
    return 0;
}